# 프로젝트 진행 기록

이 파일을 채워 10월 1일 발표용 PPT를 준비합니다. 구현하지 않은 기능은 '미구현'으로 표시하고, 심화 항목은 진행한 경우에만 작성하세요. 발표는 분석·조회·저장 시연을 포함해 5~10분입니다. 10월 1일 전후로 코드와 이 파일을 본인의 GitHub 저장소에 업로드하고, 저장소 링크를 강사에게 전달해 리뷰를 받습니다. 자세한 안내는 [진행 기록과 발표](docs/project-guide.md)에 있습니다.

## 1. 실행 방법

- JDK: 21
- IntelliJ에서 실행할 클래스: `kr.sesac.wordcounter.WordCounterApp`
- 작업 디렉터리(`pom.xml`이 있는 폴더): 프로젝트 루트 디렉터리
- 설정 위치와 현재 값: CSV 열 / TSV 열 / HTML 본문 선택자
  - CSV 열: `CsvParser`의 `TARGET_COLUMNS = List.of("text")`
  - TSV 열: `TsvParser`의 `TARGET_COLUMNS = List.of("document")`
  - HTML 본문 선택자: `#content`

## 2. 구현한 기능

| 기능 | 상태(완료·진행 중·미구현) | 확인한 입력과 결과 |
|---|---|---|
| TXT 카운팅 | 완료 | `basic.txt` 분석 결과 전체 단어 9개, 서로 다른 단어 6개 |
| CSV·TSV·HTML 처리 | 완료 | 각 기본 샘플 분석 결과 전체 단어 9개, 서로 다른 단어 6개 확인. `.htm`도 동일하게 처리 |
| 여러 파일 순차 처리 | 완료 | `samples/equivalent` 분석 결과 4개 파일 모두 성공, 전체 단어 36개, 서로 다른 단어 6개 |
| 상위 단어·특정 단어 조회 | 완료 | 빈 입력 시 상위 10개 조회 및 특정 단어 횟수 조회 기능 구현 |
| 전체 결과 저장 | 완료 | 분석 결과를 `out/counts.tsv`에 TSV 형식으로 저장 |
| 잘못된 입력·실패 파일·빈 파일 처리 | 완료 | 부분 실패 시 성공 파일만 집계하고, `samples/invalid`에서 시도 4개·성공 0개·실패 4개 확인 |

## 3. 정확성 확인과 처리 시간

- 작은 기본 샘플의 전체 결과를 정답과 비교한 방법: `basic.txt`, `basic.csv`, `basic.tsv`, `basic.html`을 각각 분석해 모두 전체 단어 9개, 서로 다른 단어 6개가 나오는지 확인했습니다.
- CSV 따옴표·줄바꿈을 확인한 결과: 따옴표 안 쉼표, 이스케이프된 큰따옴표(`""`), 따옴표 안 줄바꿈이 포함된 CSV를 각각 분석했으며 모두 정상적으로 처리되는 것을 확인했습니다.
- 일부 파일이 실패했을 때 확인한 결과: 정상 TXT, 파싱 오류 CSV, 지원하지 않는 파일을 함께 분석해 시도 2개 / 성공 1개 / 실패 1개 / 건너뜀 1개가 나오고, 정상 TXT의 결과인 전체 단어 9개 / 서로 다른 단어 6개만 집계되는 것을 확인했습니다.
- 결과 저장 파일 위치: `out/counts.tsv`

CSV의 분석 열은 `text`입니다. 아래 입력은 각각 따로 실행합니다. 정답은 [필수 요구사항의 큰 데이터 처리](docs/requirements.md#10-큰-데이터-처리)를 참고하세요.

| 입력 | 데이터 건수 / 파일 수 | 전체 단어 수 | 종류 수 | 처리 시간 | 완료·오류 |
|---|-----------------------|---|---|---|---|
| `data/klue-ynat/news-1000.csv` | 1,000 / 1             | 6,991 | 5,052 | 104.5ms | 완료 |
| `data/klue-ynat/news-10000.csv` | 10,000 / 1            | 70,374 | 28,871 | 250.3ms | 완료 |
| `data/klue-ynat/news-full.csv` | 45,678 / 1            | 321,084 | 78,309 | 411.1ms | 완료 |
| `data/klue-ynat/many` | 45,678 / 16           | 321,084 | 78,309 | 342.6ms | 완료 |

전체 파일 하나와 16개 파일의 **모든 단어별 횟수**를 비교한 방법과 결과:
  `news-full.csv`와 `many` 폴더를 각각 분석한 뒤 전체 결과를 TSV 파일로 저장해 비교했습니다.
  두 결과 모두 전체 단어 321,084개, 서로 다른 단어 78,309개였으며,
  저장된 두 TSV 파일의 전체 내용을 비교한 결과 모든 단어별 횟수가 동일함을 확인했습니다.

## 4. 구현 중 해결한 문제

### 1. 실패한 파일의 일부 분석 결과가 전체 집계에 포함되는 문제

- 문제: 여러 파일을 분석할 때 하나의 파일에서 파싱 오류가 발생하면, 오류 발생 전까지 분석된 단어가 전체 결과에 남을 수 있었습니다.
- 원인: 파일을 읽으면서 단어별 횟수를 전체 집계 Map에 바로 반영하면, 파일 처리 도중 예외가 발생하더라도 이미 추가된 결과를 되돌리기 어려웠습니다.
- 해결하거나 시도한 방법: 각 파일을 분석할 때 임시 Map에 먼저 단어별 횟수를 저장하고, 해당 파일의 분석이 정상적으로 끝난 경우에만 전체 집계 Map에 병합하도록 변경했습니다.
- 확인한 입력과 결과: 정상 TXT 파일, 파싱 오류가 발생하는 CSV 파일, 지원하지 않는 파일을 함께 분석했습니다. 시도 2개 / 성공 1개 / 실패 1개 / 건너뜀 1개로 처리되었고, 최종 결과가 정상 TXT 파일의 전체 단어 9개 / 서로 다른 단어 6개로 유지되는 것을 확인했습니다.

### 2. 존재하지 않는 경로와 분석 실패 파일의 구분

- 문제: 존재하지 않는 경로와 형식 오류가 있는 파일을 동일하게 오류 처리해 실패 원인을 구분하기 어려웠습니다.
- 원인: 파일 존재 여부를 먼저 확인하지 않고 파일 분석 과정에서 발생한 오류와 동일하게 처리했습니다.
- 해결하거나 시도한 방법: 분석 전에 파일·폴더의 존재 여부를 먼저 검사하고, 존재하는 파일에서 발생한 파싱 오류는 분석 실패로 별도 처리했습니다.
- 확인한 입력과 결과: 존재하지 않는 경로는 다시 입력받고, 형식이 잘못된 파일은 분석 실패로 집계한 뒤 프로그램이 계속 실행되는 것을 확인했습니다.

## 5. 심화(진행한 경우만)

- 한 파일 처리 개선: 바꾼 부분, 전후 시간, 결과 동일 여부
- 여러 파일 병렬 처리: 입력 폴더, 스레드 수 1·2·4, 전후 시간, 결과 동일 여부
- 중단 후 재개·데이터 수집·기타: 사용법과 확인한 결과

성능을 비교했다면 측정 기기·JDK, 예열·반복 횟수, 전체 작업의 중앙값을 적습니다. 표 양식은 [심화 요구사항](docs/advanced.md)에 있습니다.

## 6. AI 대화 또는 참고 자료

- 웹 대화에서 물어본 개념·힌트·오류 설명:
  파일 형식별 파싱 방법, 예외 처리, 여러 파일 집계 방법과 테스트 방법을 ChatGPT에 질문했습니다.

- 도움을 바탕으로 직접 구현한 내용:
  TXT·CSV·TSV·HTML 파서와 파일별 예외 처리, 임시 Map을 활용한 집계 방식 등을 직접 구현하고 수정했습니다.

- 직접 확인한 입력과 결과:
  기본 샘플과 오류 파일을 직접 실행해 결과를 확인하고, 대용량 데이터에서는 전체 파일과 16개 분할 파일의 단어별 집계 결과가 동일한지 비교했습니다.

- 참고 링크: ChatGpt

사용하지 않았다면 사용하지 않았다고 적으면 됩니다.

## 7. 발표할 내용

- 구현한 기능과 전체 처리 흐름
- 시연할 파일·폴더와 정답
- 분석 → 조회 → 저장 시연
- 해결한 문제 또는 성능 실험에서 알게 된 점
- 남은 문제와 더 개선하고 싶은 부분
